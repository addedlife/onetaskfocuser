{
  "rules": {
    ".read": false,
    ".write": false,
    "phone-relay": {
      "commands": {
        ".read": "auth != null && auth.token.relay_device === true",
        ".write": "auth != null && auth.token.relay_device === true"
      }
    }
  }
}
