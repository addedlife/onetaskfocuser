# Live site snapshots (downloaded by sync script, not part of the project)
_live_snapshot/

# Deploy staging area
deploy_staging/

# Temp deploy files
deploy_temp.zip

# Netlify local state
.netlify/

# Claude Code project files
.claude/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Backup/recovery files that shouldn't be in the repo
v4_restore_state.json
status.json
status_cmd.txt
netlify_status.txt
pending_restore.json
DEPLOY_LOG.json

# Private admin/recovery tools — never serve publicly
HANDOFF.md
merge.html
recover.html
restore_data.html
quick_restore.html
safe-deploy.ps1
sync-from-live.ps1
tools/

# Old deploy scripts (deprecated, kept locally but not tracked)
deploy.bat
deploy_api.ps1
check_deploy.ps1
