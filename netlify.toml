[build]
  base = "apps/web"

# Auth no longer routes through Netlify. The app is deployed to Firebase Hosting
# (.github/workflows/deploy.yml) and Firebase Auth's /__/auth handler is served same-origin
# on the canonical onetaskonly-app.web.app. No Netlify auth proxy is needed.
