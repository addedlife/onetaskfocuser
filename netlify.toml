[build]
  base = "apps/web"

# Auth no longer routes through Netlify. The app is deployed to Firebase Hosting
# (.github/workflows/deploy.yml); sign-in happens on the canonical firebaseapp.com origin
# where Firebase Auth's /__/auth handler is served same-origin. No Netlify auth proxy needed.
