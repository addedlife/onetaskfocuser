# Current State

Last updated: 2026-05-08

This is the active OneTask / Switchboard / NerveCenter repo and Netlify deploy root.

Live app:

- Production URL: `https://onetaskfocuser.netlify.app`
- Latest verified production deploy: Netlify automatic production deploy from latest pushed `main`
- Latest served asset after deploy: `assets/index-BoHqrUDj.js`
- Git branch: `main`
- Latest deployed source: latest pushed `main`

Current product truth:

- NerveCenter is the operating front door for tasks, Shailos, and phone surfaces.
- DeskPhone Web parity is now judged by real browser behavior, not native handoff shortcuts. Visible web-phone buttons should either complete in-browser or call a host endpoint directly.
- Google Calendar/Gmail connector code is present and visible, but production Netlify currently has no `GOOGLE_CLIENT_ID`.
- If no Google Client ID exists, the app now shows a setup path instead of silently hiding the connector.
- Google token is persisted to localStorage (`ot_google_token`, `ot_google_token_expiry`, `ot_google_connected`). Silent re-auth fires automatically on load if expired (token stored at 3300s / 55 min). 401 handlers clear localStorage before nulling token. `googleWasConnected` flag shows a "Reconnecting…" spinner for 6 seconds, then surfaces a clickable "Reconnect Google" button if silent re-auth didn't complete.
- OAuth scope is `calendar` (full) + `gmail.readonly`. calendarList endpoint is `users/me/calendarList`. If calendarList fails for any reason, falls back to `primary` only.
- Calendar card: up to 20 events from all subscribed calendars, timed events first then all-day at bottom, each row is a clickable link. "+" opens a natural-language Add Event modal (AI parses to Calendar JSON → POST). "↗" opens Google Calendar. Refresh and disconnect buttons in header.
- Gmail card: up to 20 messages — personal (all) + promotions/updates (important only), most recent first, no read/unread filter. Each row: bold sender + date, then a one-sentence AI summary of the email body content (batch AI call, ≤10 words). Hover tooltip shows full from/subject/snippet. Whole row is a clickable link to the message. Disconnect in Calendar header removes both cards.
- NerveCenter phone section is a vertical split — texts top, calls bottom, each independently scrollable. The compact dashboard phone surface now uses a much flatter one-line connection/status strip instead of the older tall connected-phone header.
- AppSuiteChrome left rail and DeskPhone Web left rail both auto-collapse after 10 seconds of inactivity. Toggle at bottom of each. On 2026-05-06, both timer paths were repaired so turning the feature on or opening the rail starts the countdown without requiring a mouse-leave event first.
- Top-left menu button and PostItStack now track `sidebarW` so they are never hidden behind the sidebar.
- DeskPhone Web message history, MMS clean image bubbles, fullscreen image rotate/close, and splitters were smoke-tested before the latest deploy.
- DeskPhone Web now shows native outgoing send states such as `Confirming on phone`; deployed after native DeskPhone b249 exposed the host API fields.
- DeskPhone Web local source now keeps open-message bubble actions as clean direct icons on the bubble, without boxed menu rows, and protects bottom-of-thread scrolling so the newest bubble/action strip is not hidden below the screen. Selected-conversation header actions remain consolidated into a three-dot menu with icon plus function label per row. This is verified locally by `npm run build` but has not been pushed or deployed.
- DeskPhone Web local source now removes the native-app redirect layer from visible web-phone parity controls: conversation row/header mark read, mark unread, pin, mute, and block call direct host endpoints; call-history block/delete/delete-all call direct host endpoints; typed-number/contact text actions open the browser New Message composer; contact New/Edit stays inside the browser contact editor; Settings no longer exposes "Show native app"; Developer Tools navigation stays in the browser and calls host endpoints. The matching native host support shipped in DeskPhone `b261`. Verified locally by `npm run build` and `node artifacts/deskphone-web-button-audit.cjs`; `npm run lint` is currently blocked by the repo's ESLint 9 glob/config issue, not by this slice.
- DeskPhone Web local source now has the first GV-style visual cleanup pass: white canvas tokens, muted selected rows, teal communications accent, quieter icon buttons, flexible low-emphasis message/call filters, lighter typography, reduced blue fill usage, and single-owner pane dividers. Verified locally by `npm run build` and `node artifacts/deskphone-web-button-audit.cjs`. A temporary screenshot was captured for visual QA but not committed.
- Current local source now adds `Google Voice` as an Appearance theme, keeps the existing color-scheme selector, and starts applying the same clean rules across NerveCenter without hard-locking every screen to white. NerveCenter/WebPhone now derive quiet borders, surfaces, text weights, and accent behavior from the active scheme. WebPhone now receives the active `T` theme and maps it into CSS variables, so changing Appearance should affect the WebPhone page. Verified by `npm run build` and `node artifacts/deskphone-web-button-audit.cjs` after starting a temporary dev server on port `4183`.
- NerveCenter local source now has a broader Google-current visual polish pass: wider/cleaner app rail, more page padding, 16px panel/card gutters, lighter 400/500 typography, quieter toolbar/action buttons, reduced row divider/gridline feel, calmer Google Calendar/Gmail strip, cleaned More Actions drawer, and less boxy Settings modal tabs/surfaces. Verified by `npm run build`; headless screenshot QA stayed on the app's local data-loading screen in a fresh browser profile, so visual inspection should be done in the authenticated local browser before release.
- Follow-up local QA waited through startup and confirmed the NerveCenter dashboard renders in a fresh browser profile. The Firebase-unreachable banner was overlapping the fixed NerveCenter shell; local source now offsets the left rail and NerveCenter panel below active warning bars. Verified by `npm run build` and screenshot artifact `artifacts/nervecenter-local-qa-2026-05-08-offset-fixed.png`.
- NerveCenter local source now has a readability/spacing polish pass based on the GV clean-design rulebook plus Material-style scale targets: NerveCenter working rows use larger 16px body text, 14px metadata, 18px section titles, wider 20px panel gaps, taller 40-44px controls, and more 56px+ row rhythm. The phone mini-surface, Settings modal, and WebPhone shared phone UI also received larger text/control baselines so the clean look carries across the app instead of only the NerveCenter front door. Verified by `npm run build` and `git diff --check`; Edge headless screenshot still stops at the app's fresh-profile `Loading...` screen, so final visual approval should happen in the authenticated local browser. In this session, Vite started the fresh dev server at `http://127.0.0.1:4192/?suite=nervecenter` because `4191` was already in use.
- NerveCenter local source now removes the duplicate `Shaila tasks` strip from the bottom of the Tasks column so questions only live in the dedicated Shailos column on the dashboard while the full queue still keeps the underlying shaila task steps. The conversation-capture review modal also now lets the user change an extracted item's destination (`Task`, `Shaila`, `Schedule`, `Reminder`) before adding it, and the parser prompt now explicitly keeps "move up / prioritize this task" requests in tasks instead of misfiling them as shailos. Verified by `npm run build`.
- `Chief of Staff` has been removed from the OneTask/NerveCenter surface after repeated white-screen/takeover regressions. The left-rail entry, popup/direct route handling, in-app overlay, sweep panel, local Chief memory key usage, and Chief QA artifacts were removed. Verified by `npm run build`, `git diff --check`, and `rg` checks showing no remaining Chief code or artifact references.
- DeskPhone Web now has an Android mobile-bridge mode: Android bridge status or a mobile/coarse-touch browser using `http://127.0.0.1:8765` adds an `is-mobile-bridge` layout class, uses a compact top rail on Android/coarse mobile screens, hides the call-history side pane from the message view to avoid squashing, labels reconnect as `Refresh phone data`, and treats localhost as a local bridge API rather than Bluetooth phone/device management. In Android bridge mode, Settings shows only Bridge/Appearance, removes Bluetooth Settings and saved/scanned/paired device lists, and uses local-bridge/text wording instead of preferred/saved-phone wording while preserving normal Windows-host device controls on desktop loopback. It also tolerates partial bridge endpoint failures so a `/messages` error no longer clears the whole phone surface. Verified by `npm run build`, `node artifacts/deskphone-web-button-audit.cjs`, and a focused bridge-mode text audit.
- Follow-up fix: Android bridge readiness now means the bridge API is reachable, not that remote phone data rows are already cached. The tablet's real `/status` response can report `connected:false` and zero remote contacts/calls/messages while still being a healthy `Android Bridge 0.1.12` API with Android SMS/dialer available. WebPhone now shows `Bridge connected` in that case, keeps Bluetooth device-management controls hidden, and accepts `127.0.0.1`, `localhost`, and IPv6 loopback as local bridge hosts on port `8765`.
- The GV clean-design rulebook is at `C:\Users\ydanz\OneDrive\Documents\Shamash Pro 3\docs\GV_CLEAN_DESIGN_RULES.md`, not under `sandbox\docs`. Use that control-folder doc as the design source while editing sandbox code.
- WebPhone screen layout is now cleaner: removed redundant "Make Call" and "Calls" navigation buttons (left rail now shows only Phone/Messages, Contacts, Settings). Call history in the message view now always displays the full call list (not filtered by contact), creating a unified "Messages & Calls" surface. The "Open full call history" button is removed from the call pane header. Dialer remains always accessible in the right pane.
- WebPhone connection status UI now separates the real two-step relationship in consumer language: (1) "This screen to [this device/name/type]" and (2) "[this device/name/type] to phone" with Bluetooth called out only for the device-to-phone leg. The rail and Settings page no longer say Host URL, API, endpoint, local bridge, or Bluetooth connector. Status refresh remains automatic every 5 seconds and the UI says so. The rail check button now only rereads status; reconnect is a separate phone-management action. Bluetooth scan/connect controls are hidden unless the local phone service is online and reports device-management fields, and are tucked under "Pair or switch phones" because they are real Windows-host controls, not browser Bluetooth controls. Mobile/local phone-service mode only shows check/open-service actions. Verified locally by `npm run build`.
- DeskPhone Web now has a stricter Android phone-view layout protocol for mobile bridge mode. Android/coarse phone screens use a single-viewport contract with top navigation, contained pane scrolling, safe-area padding, no hidden horizontal overflow, touch-sized controls, bottom-safe compose spacing, fixed in-viewport action menus, and a master/detail message flow where tapping a conversation moves into the thread instead of keeping the list and chat mashed together. Verified by `npm run build`, headless Chrome Android screenshots `artifacts/deskphone-android-phoneview-harness-393x852-final.png` and `artifacts/deskphone-android-phoneview-thread-393x852.png`, and measured viewport/scroll width equality in the temporary local harness.
- A disconnected `src/08-app-split/` lab now exists for the large `src/08-app.jsx` split. The live app still imports `src/08-app.jsx`; the lab exports the same `App` shape through `src/08-app-split/index.jsx` for future temporary-link testing. Extracted modules: `ui-tokens.jsx`, `components/AppSuiteChrome.jsx`, `components/NerveCenterPanel.jsx`, `components/NerveCenterPhoneSurface.jsx`, `components/SuitePanels.jsx`, `components/DeskPhoneMiniDock.jsx`, `components/ConvCapture.jsx`, `hooks/useAppConfig.js`, `hooks/useAppTheme.js`, `hooks/useBlockedResumeNudge.js`, `hooks/useClockTicks.js`, `hooks/useDeskPhoneBridge.js`, `hooks/useGoogleWorkspace.js`, `hooks/useInsightsAi.js`, `hooks/useInsightsMetrics.js`, `hooks/useMrsWLivePriority.js`, `hooks/useOfflineStatus.js`, `hooks/usePendingRecordings.js`, `hooks/useQueueDerivations.js`, `hooks/useQueueToast.js`, `hooks/useShailosFrameMessages.js`, `hooks/useShailosSharedState.js`, `hooks/useStaleTaskNudge.js`, `hooks/useSuiteNavigation.js`, `hooks/useTimedUndo.js`, `hooks/useTipCarousel.js`, and `hooks/useToastNotifier.js`. The Google hook keeps the existing Calendar/Gmail behavior but adds proactive silent token refresh before expiry to reduce avoidable re-auth prompts. The lab bundle compiles with `npx esbuild src/08-app-split/index.jsx --bundle --format=esm --jsx=automatic`, and the normal live-path `npm run build` still passes.

- Call history now displays contact names by matching phone numbers against the contacts list. The `enrichCallWithContactName()` function normalizes call numbers and finds matching contacts, populating display names. Fallback is phone number if no contact match. Applied system-wide to all call history views (message pane right panel, full calls tab, call actions). Verified locally by `npm run build`.

Current repo condition:

- The current local source includes WebPhone screen layout, connection settings UI, and Android phone-view fixes: removed redundant call buttons, unified call history display in message view, replaced technical connection labels with plain English, and added a stricter Android single-viewport responsive contract so the mobile bridge layout does not mash panes or extend sideways. The latest local connection copy now treats localhost as the device phone-service target, not as a browser Bluetooth connector; it separates "check current status" from "reconnect phone" and moves real scan/connect controls under an advanced "Pair or switch phones" disclosure. Build passes, and `node artifacts/deskphone-web-button-audit.cjs` was refreshed for the current unified call-pane shape and now passes with zero `/handoff` calls. Ready for real-device Android browser QA before deployment.
- The `08-app` split work is isolated in `src/08-app-split/` and has not been linked into the live app. Current reduction: split lab `App.jsx` is about 2,945 lines versus the live `src/08-app.jsx` at about 5,829 lines. Next step is to keep extracting feature modules, then temporarily point `src/00-auth.jsx` to `./08-app-split/index.jsx` for a full Vite/browser test only when the split is complete enough to evaluate.
- The latest pushed `main` now includes the verified auto-collapse timer repair for both app rails and the current Netlify production deploy should come from that pushed branch.
- The latest pushed `main` also includes the DeskPhone Web browser-parity fixes for no-op/native-redirect buttons now that native DeskPhone `b261` is released and the focused button audit passed with zero `/handoff` calls.
- The current local DeskPhone Web source includes the first GV-style visual pass. The production build passes, and the focused WebPhone button audit still passes with zero `/handoff` calls. The older broad parity smoke harness timed out during this session and should be treated as a harness follow-up before relying on it for this visual lane.
- The NerveCenter/theme-sync and polish slices are now pushed to `main`. The local code passed `npm run build`, but visual approval still needs a real authenticated browser pass across at least `Google Voice` plus one older scheme.
- Follow-up capture-routing fix: the review modal now honors the user's edited shaila wording first instead of saving the older AI synopsis when those two differ.
- Android bridge APK `0.1.12` / versionCode `13` has been built locally at `C:\Users\ydanz\OneDrive\Documents\Shamash Pro 3\android-webphone-bridge\dist\WebPhoneAndroidBridge-debug.apk` and installed successfully on the tablet in the prior session. This APK keeps `/messages` as a successful JSON response when remote MAP has no rows and Android `READ_SMS` is missing, returning the bridge error in `lastError` instead of causing the web page to treat the whole phone host as failed.
- DeskPhone Web parity map review is complete: 199 of 199 action rows reviewed as of 2026-05-06 (193 implemented-web, 0 host-api-needed, 6 native-only, 0 not-yet-reviewed). Latest runtime slices add browser Choose Device routing to Settings, host-backed build update accept/snooze/show controls, host-backed active-call mute, browser message Forward into a prefilled New Message draft, browser message Pin plus pinned-message jump behavior through native b256 host support, browser message Delete plus Undo through native b257 host support, browser Settings saved/scanned device controls through native b258 host support, browser contact save/delete plus call-history Undo through native b259 host support, and browser reply/full-compose attachments through native b260 `/send-with-attachments` support.
- Many untracked artifacts/docs still exist from prior work. Disposable untracked `artifacts/*dev*.log` files were cleaned where not locked; the remaining `nervecenter-qa-dev` and `nervecenter-readability-dev` logs are held open by pre-existing Vite dev servers on port `4191`. Generated docs, backups, and screenshot artifacts were left in place rather than deleting possible project evidence.
- `shailos/` is generated output copied into the deploy; do not treat it as editable source.

High-risk areas:

- `src/08-app.jsx` is large and central; touch it narrowly. Prefer continuing inside the disconnected `src/08-app-split/` lab until temporary-link testing is intentionally scheduled.
- The latest NerveCenter clean-style work is partial by design: it covers the visible NerveCenter panel shell, phone mini-surface, theme selector, and WebPhone theme variable bridge. Next session should visually inspect NerveCenter and WebPhone across at least Google Voice plus one existing color scheme before pushing/deploying.
- Google connector behavior depends on Netlify environment variables and Google OAuth setup, not just UI code.
- Firebase writes in local dev hit the real account.
