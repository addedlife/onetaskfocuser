Shamash Pro 4 — context kernel. Read this first, then only the matching row in docs/ops/CONTEXT_INDEX.md.

FACTS
- Live: https://onetaskfocuser.netlify.app | Origin: github.com/addedlife/onetaskfocuser | Push target: origin/main
- Source: C:\Users\ydanz\OneDrive\Documents\Shamash Pro 4 App | Prod dirs: apps/web, apps/shailos, apps/phone-host-windows (others = archive)
- Firebase: onetaskonly-app | uid = email prefix lowercased (rabbidanziger), NOT auth uid | data under users/{uid}/
- Release: push origin/main → Netlify auto-builds apps/web (root netlify.toml). Manual `npx netlify deploy` only on failure. Don't relink Netlify.
- Gates: apps/web `npm run build`; apps/shailos `npm run build`; apps/phone-host-windows `dotnet build`. Smoke affected surface.
- Before edits: check docs/ops/VERIFICATION_LOG.md. Log results there, migration decisions in MIGRATION_MANIFEST.md.

FINDS & FIXES — append-only ledger. One line each. Format:
  YYYY-MM-DD [area] symptom → root cause → fix (file:loc)
Keep terse: no prose, no blank lines between entries, newest at bottom. Promote anything load-bearing into FACTS/CONTEXT_INDEX and delete the line.
- 2026-06-07 [phone] SMS reply silently failed → post() ignored relay HTTP errors, still cleared compose → return ok flag, gate clear on success (NerveCenterPhoneSurface.jsx post/sendSms)
- 2026-06-07 [nervecenter] summary line showed AI text twice → supercrunch short-circuited to chiefSummaryText (same as nextActionBar) → supercrunch always builds from items; bar shows only ⚡action (NerveCenterPanel.jsx)
- 2026-06-07 [nervecenter] calendar items showed "Event" fallback → read ev.summary on rows that only have ev.evt → use ev.evt.* (calRich/supercrunch)
- 2026-06-07 [calendar] all-day events → label unified to "(all day)" everywhere
